#Instructions:
#1 Place downloaded student folder into the input folder
#2 Enter a bash terminal and navigate into folder containing the file
#3 In the command terminal enter the following text below and hit the return key:
chmod +x docker-run.sh && ./docker-run.sh
#4 the output folder will have the attendance report

